type Props = {
  data: Data
}

type Data = {
  title: string,
  version: string,
  desc: string,
  img: string,
  link: string,
}

const Card = (props: Props) => {
  return (
    <>
    </>
  )
}

export default Card;